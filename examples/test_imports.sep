export { base_pattern, utility_function }

pattern base_pattern {
    value = 42
    print("Base pattern executed")
}

function utility_function(x) {
    return x * 2
}

import "other_module.sep" { helper_pattern }
import "utils.sep"

pattern main_pattern {
    local_value = utility_function(10)
    print("Local value: ", local_value)
}
