// Test async function declarations and await expressions

async function fetchData(source) {
    x = 42
    y = await measure_entropy(source)
    return x + y
}

function normalFunction() {
    return "normal result"
}

// Test calling async function
result1 = fetchData("test_pattern")
result2 = normalFunction()

// Test await expression directly
pattern test_await {
    direct_await = await measure_coherence("direct_test")
}
