pattern test_while_and_functions {
    // Test while loop
    x = 0;
    while (x < 5) {
        x = x + 1;
    }
    
    // Test function declaration and call
    function double_it(n) {
        return n * 2;
    }
    
    y = double_it(x);
    z = double_it(10);
}
